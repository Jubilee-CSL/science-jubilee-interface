// ─── Placement Jubilee ──────────────────────────────────────
// coordinates (JSON) : (44.55, 41.79, 3.00) mm
// Rotation utilisateur : 0°
// Labware dims : xDim=127.76, yDim=85.57

translate([44.548, 41.787, 3.000])
rotate([0, 0, 0.0])
{
    // ═══════════════════════════════════════════════════════════════════
    // 3-D model: Agilent 1 Well Reservoir 290 mL
    // Load name: agilent_1_reservoir_290ml
    // Generated by labware_to_scad.py
    // Well shape(s): rectangular
    // ═══════════════════════════════════════════════════════════════════

    // ── Render quality ─────────────────────────────────────────────────
    $fn = 64;   // circle/cylinder facets

    // ── Plate dimensions ────────────────────────────────────────────────
    plate_x = 127.76;
    plate_y = 85.57;
    plate_z = 44.04;

    // ── Corner rounding radius (set to 0 for sharp corners) ─────────────
    corner_r = 3.0;

    // ── Model ───────────────────────────────────────────────────────────
    difference() {

        // Outer plate body (rounded corners via hull)
        hull() {
            for (cx = [3.0, 124.76],
                 cy = [3.0, 82.57])
                translate([cx, cy, 0])
                    cylinder(r=3.0, h=44.04);
        }

        // ── Well cutouts ───────────────────────────────────────
        // Well A1
        translate([9.880000000000003, 6.784999999999997, 4.81])
            cube([108, 72, 39.23 + 1]);
    }

}
